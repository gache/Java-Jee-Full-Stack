function afficheChamp(form) {
  console.log(`Le nom de la personne est: ${form.nom.value}`);
  console.log(`Le prenom de la personne est: ${form.prenom.value}`);
  console.log(`Le email de la personne est: ${form.email.value}`);
  console.log(`Le mdp de la personne est: ${form.mdp.value}`);
  console.log(`Le vmp de la personne est: ${form.vmp.value}`);
}
